node_modules/
dist/
web/dist/
.DS_Store
*.log

# Runtime job data (uploaded videos, frames, COLMAP, splats) — can be large
workspace/

# Brush binary is fetched per-platform by `npm run setup`
tools/brush/

# COLMAP is auto-fetched on Windows by `npm run setup`
tools/colmap/

# Local agent/skill scratch — not part of the app
.agents/

# Generated/experimental splats, but ship the bundled sample sculpture
samples/*.ply
!samples/sample.ply
!samples/sample-scene.ply

.env
.vite/

# Playwright MCP test artifacts
.playwright-mcp/

# Skill-manager artifacts (local tooling, not the app)
.claude/
.agents/
skills-lock.json
